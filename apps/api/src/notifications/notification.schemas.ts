import{z}from'zod';export const tokenInput=z.object({token:z.string().startsWith('ExponentPushToken[').max(300),platform:z.enum(['IOS','ANDROID']),deviceName:z.string().max(120).optional()});export const preferenceInput=z.object({transactionalPush:z.boolean(),transactionalEmail:z.boolean(),transactionalSms:z.boolean(),marketingPush:z.boolean(),marketingEmail:z.boolean(),quietHoursStart:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).nullable(),quietHoursEnd:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).nullable(),timezone:z.string().min(3).max(60),digestMode:z.enum(['IMMEDIATE','DAILY','WEEKLY','OFF'])});
