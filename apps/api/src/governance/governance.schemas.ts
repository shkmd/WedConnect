import{z}from'zod';export const privacyInput=z.object({type:z.enum(['EXPORT','CORRECTION','DELETION','CONSENT_WITHDRAWAL']),details:z.string().min(10).max(2000).optional()});export const privacyDecisionInput=z.object({status:z.enum(['VERIFYING','PROCESSING','COMPLETED','REJECTED']),resolution:z.string().min(10).max(2000)});export const analyticsNames=['city_selected','category_selected','search_performed','filter_applied','zero_result_search','vendor_impression','sponsored_impression','vendor_profile_opened','portfolio_viewed','package_viewed','vendor_saved','comparison_started','enquiry_submitted','contact_consent_granted','contact_revealed','phone_clicked','whatsapp_clicked','lead_viewed','lead_responded','credit_used','subscription_started','promotion_purchased','report_submitted','vendor_approved']as const;export const analyticsInput=z.object({name:z.enum(analyticsNames),anonymousSessionId:z.string().min(16).max(120),properties:z.record(z.string(),z.union([z.string().max(160),z.number(),z.boolean(),z.null()])),occurredAt:z.coerce.date()});
