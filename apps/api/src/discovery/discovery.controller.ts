import{Body,Controller,Get,Inject,Param,Post,Query,UseGuards}from'@nestjs/common';import{AuthGuard}from'../identity/auth.guard';import{PermissionGuard,RequiresPermission}from'../identity/permission.guard';import{DiscoveryService}from'./discovery.service';
@Controller('discovery')export class DiscoveryController{constructor(@Inject(DiscoveryService)private readonly discovery:DiscoveryService){}@Get('cities')cities(){return this.discovery.cities()}@Get('categories')categories(@Query('city')city?:string){return this.discovery.categories(city)}@Get('search')search(@Query()query:unknown){return this.discovery.search(query)}@Get(':city/:category/:vendor')vendor(@Param('city')city:string,@Param('category')category:string,@Param('vendor')vendor:string){return this.discovery.vendor(city,category,vendor)}}
@Controller('admin/sponsored-placements')@UseGuards(AuthGuard,PermissionGuard)@RequiresPermission('sponsored.manage')export class SponsoredController{constructor(@Inject(DiscoveryService)private readonly discovery:DiscoveryService){}@Post()create(@Body()body:unknown){return this.discovery.createSponsored(body)}}
