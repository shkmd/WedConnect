export type UploadProgress={sent:number;total:number;percent:number};export function uploadWithProgress(url:string,uri:string,mimeType:string,onProgress:(x:UploadProgress)=>void,retries=2):Promise<void>{return new Promise((resolve,reject)=>{const run=(left:number)=>{const xhr=new XMLHttpRequest();xhr.open('PUT',url);xhr.setRequestHeader('Content-Type',mimeType);xhr.upload.onprogress=e=>onProgress({sent:e.loaded,total:e.total,percent:e.lengthComputable?Math.round(e.loaded/e.total*100):0});xhr.onload=()=>xhr.status>=200&&xhr.status<300?resolve():left?run(left-1):reject(new Error(`Upload failed (${xhr.status})`));xhr.onerror=()=>left?run(left-1):reject(new Error('Upload failed'));xhr.send({uri,type:mimeType,name:'portfolio-upload'}as unknown as Blob)};run(retries)})}
