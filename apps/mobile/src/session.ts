import*as SecureStore from'expo-secure-store';const KEY='wedconnect.session';export type MobileSession={accessToken:string;role:'CUSTOMER'|'VENDOR_OWNER'|'VENDOR_STAFF';userId:string};export async function saveSession(x:MobileSession){await SecureStore.setItemAsync(KEY,JSON.stringify(x),{keychainAccessible:SecureStore.WHEN_UNLOCKED_THIS_DEVICE_ONLY})}export async function loadSession(){const x=await SecureStore.getItemAsync(KEY);return x?JSON.parse(x)as MobileSession:null}export async function clearSession(){await SecureStore.deleteItemAsync(KEY)}
