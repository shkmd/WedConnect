import Constants from'expo-constants';import*as Device from'expo-device';import*as Notifications from'expo-notifications';export async function requestPushToken(){if(!Device.isDevice)return{enabled:false as const,reason:'physical-device-required'};const existing=await Notifications.getPermissionsAsync(),permission=existing.granted?existing:await Notifications.requestPermissionsAsync();if(!permission.granted)return{enabled:false as const,reason:'permission-denied'};const projectId=Constants.expoConfig?.extra?.eas?.projectId??Constants.easConfig?.projectId;if(!projectId)return{enabled:false as const,reason:'eas-project-id-required'};const token=await Notifications.getExpoPushTokenAsync({projectId});return{enabled:true as const,token:token.data}}
