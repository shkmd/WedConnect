import{useLocalSearchParams}from'expo-router';import{SafeAreaView,Text,View}from'react-native';import{ui}from'../../src/ui';export default function VendorDeepLink(){const{slug}=useLocalSearchParams<{slug:string}>();return <SafeAreaView style={ui.safe}><View style={ui.screen}><Text style={ui.eye}>VENDOR PROFILE</Text><Text style={ui.title}>{slug?.replaceAll('-',' ')}</Text><Text style={ui.body}>This profile was opened from a secure WedConnect deep link.</Text></View></SafeAreaView>}
