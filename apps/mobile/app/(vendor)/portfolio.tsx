import{useState}from'react';import*as ImagePicker from'expo-image-picker';import{Image,SafeAreaView,Text,View}from'react-native';import{ui}from'../../src/ui';export default function Portfolio(){const[uri,setUri]=useState<string|null>(null);async function camera(){const p=await ImagePicker.requestCameraPermissionsAsync();if(!p.granted)return;const r=await ImagePicker.launchCameraAsync({mediaTypes:['images','videos'],quality:.85});if(!r.canceled)setUri(r.assets[0]?.uri??null)}async function gallery(){const r=await ImagePicker.launchImageLibraryAsync({mediaTypes:['images','videos'],quality:.85});if(!r.canceled)setUri(r.assets[0]?.uri??null)}return <SafeAreaView style={ui.safe}><View style={ui.screen}><Text style={ui.title}>Portfolio upload</Text><Text style={ui.body}>Camera permission is requested only when you choose the camera. Gallery selection stays separate.</Text>{uri?<Image accessibilityLabel="Selected portfolio preview" source={{uri}} style={{height:220,borderRadius:16}}/>:null}<Text accessibilityRole="button" onPress={()=>void camera()} style={ui.button}>Open camera</Text><Text accessibilityRole="button" onPress={()=>void gallery()} style={ui.outline}>Choose from gallery</Text><Text style={ui.muted}>Uploads use signed URLs, progress reporting and two automatic retries.</Text></View></SafeAreaView>}
