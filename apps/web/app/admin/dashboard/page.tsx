import { DashboardShell, MetricCard } from '../../components/dashboard-shell';
import { DashboardState } from '../../components/dashboard-state';
import { apiGet } from '../../lib/server-api';
type AdminData={metrics:{pendingVendors:number;openReports:number;openGrievances:number;pendingReviews:number;pendingMedia:number;privacyRequests:number;users:number;requirements:number};queue:Array<{type:string;count:number;href:string}>};
export default async function AdminDashboard(){const result=await apiGet<AdminData>('/dashboard/admin');return <DashboardShell role="admin" active="Overview" eyebrow="Platform operations" title="Command centre">{!result.ok?<DashboardState title="Restricted operations workspace" message={result.message} role="ADMIN"/>:<><section className="ops-alert"><span>●</span><div><strong>Live platform data connected</strong><p>Metrics below are calculated from the production-shaped Supabase schema.</p></div><small>Current snapshot</small></section><section className="metric-grid"><MetricCard label="Pending vendors" value={String(result.data.metrics.pendingVendors)} detail="Awaiting moderation"/><MetricCard label="Open trust cases" value={String(result.data.metrics.openReports+result.data.metrics.openGrievances)} detail={`${result.data.metrics.openReports} reports · ${result.data.metrics.openGrievances} grievances`} tone="gold"/><MetricCard label="Platform users" value={result.data.metrics.users.toLocaleString('en-IN')} detail="Registered accounts" tone="green"/><MetricCard label="Requirements" value={result.data.metrics.requirements.toLocaleString('en-IN')} detail="Customer requirements" tone="blue"/></section><div className="dash-two-col admin-columns"><section className="dash-card"><div className="card-heading"><div><p className="dash-kicker">Needs attention</p><h2>Operations queue</h2></div><a href="/admin/trust">Open trust console →</a></div><div className="ops-queue">{result.data.queue.map((item,index)=><article key={item.type}><span className={`queue-icon q-${index%4}`}>{index===0?'✓':index===1?'!':'◇'}</span><div><strong>Moderation queue</strong><h3>{item.type}</h3><p>{item.count?`${item.count} records require attention`:'Queue is clear'}</p></div><time>{item.count}</time><a className="queue-review" href={item.href}>Review</a></article>)}</div></section><section className="dash-card"><div className="card-heading"><div><p className="dash-kicker">Current totals</p><h2>Content health</h2></div></div><dl className="ops-stats"><div><dt>Pending reviews</dt><dd>{result.data.metrics.pendingReviews}</dd></div><div><dt>Pending media</dt><dd>{result.data.metrics.pendingMedia}</dd></div><div><dt>Privacy requests</dt><dd>{result.data.metrics.privacyRequests}</dd></div><div><dt>Trust reports</dt><dd>{result.data.metrics.openReports}</dd></div></dl></section></div></>}</DashboardShell>}
